#include <stdio.h>
#include "main.h"
/**
 * add - check that adds two integers and returns the result
 * @a: check the integer
 * @b: check the integer
 * Return: the result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
